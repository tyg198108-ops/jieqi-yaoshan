# 部署上线手册（腾讯云开发 · 企业账号）

> 环境信息（已确认）
> - 环境 ID：`jieqi-yaoshan-d7gd6ypfscd10299f`
> - 地域：上海（ap-shanghai）
> - 账号主体：企业（公司认证）
> - 推荐套餐：**个人版 ¥19.9/月**（约 240 元/年，超出自动转按量，不会停服）

整条链路分三段，**顺序不能乱**：

```
数据库（存用户方案）  →  登录授权（给访客身份）  →  HTTP 访问服务（后端变成网址）
       ①                        ②                          ③
                                                            ↓
                                                   填回 apiBase → 重新部署前端 ④
```

---

## 第 ① 步：建数据库集合

存用户保存的席单。不用设计表结构，建两个"集合"就行。

1. 打开 https://console.cloud.tencent.com/tcb → 点进环境 `jieqi-yaoshan-d7gd6ypfscd10299f`
2. 左侧菜单 → **数据库**
3. 点 **新建集合**
4. 集合名填 **`user_plans`** → 确定
5. 同样方法再建一个 **`user_profiles`**

**接着必须改权限**（不改的话，别人能看到你保存的方案）：

6. 点开刚建的 `user_plans` → 右上角 **权限设置**
7. 选 **仅创建者可读写** → 确定
8. `user_profiles` 做同样设置

> 找不到"权限设置"：它在集合详情页右上角，图标像个盾牌/齿轮，也可能叫"安全规则"。
> 选到"仅创建者可读写"后，系统会自动给每条记录加 `_openid` 字段做隔离，
> 我们前端代码里不用写任何判断。

---

## 第 ② 步：开匿名登录

目的是让访客**不用注册**也能用一个随机 ID 保存自己的方案。首次使用零门槛。

1. 左侧菜单 → **身份认证**（有的版本叫"登录授权"）
2. 找到 **匿名登录** → 开关打开
3. 顺手把 **手机号登录** 也打开（现在用不上，但你以后想升级不用重部署）

> 如果这里提示需要开通短信服务才能勾手机号登录，先跳过手机号，只开匿名登录即可。

**另外补一件事**（容易漏）：

4. 左侧菜单 → **环境设置** → **安全配置**（或直接搜"安全域名"）
5. 在 **WEB 安全域名** 里加上你要用的两个域名：
   - 静态托管的免费域名（第 ④ 步拿到后回来填）
   - `localhost:8000`（本地调试用）

> 不填的话，浏览器会报"域名不在白名单"，表现为：页面能打开，「我的方案」点开却是空的并提示未授权。

---

## 第 ③ 步：把后端变成网址（这一步最关键）

云函数默认没有对外网址，要通过 **HTTP 访问服务** 暴露出来。

1. 左侧菜单 → **HTTP 访问服务**（也可能在"云函数"分组下）
2. 点 **新建**
3. **路径**填：`/api/*`（或直接 `/api`）
4. **关联资源**选：云函数 → `yaoshan-api`
5. 确定

建好后页面会显示一个域名，形如：

```
https://jieqi-yaoshan-1a2b3c.service.tcloudbase.com
```

**把这个域名抄下来**，下一步要用。

> 找不到 HTTP 访问服务？用控制台顶部的搜索框搜"HTTP"，能直接跳过去。

---

## 第 ④ 步：填回后端地址，重新部署

后端现在有网址了，但前端还不知道它在哪。

1. 打开项目里的 `js/cloud-config.js`
2. 把 `apiBase` 那行的 `null` 改成刚才抄的域名：

```js
window.YS_CLOUD_CONFIG = {
  enabled: true,
  env: 'jieqi-yaoshan-d7gd6ypfscd10299f',
  region: 'ap-shanghai',
  apiBase: 'https://jieqi-yaoshan-1a2b3c.service.tcloudbase.com',   // ← 改这行
  collection: { plans: 'user_plans', profiles: 'user_profiles' }
};
```

3. 双击 **`部署到腾讯云.bat`**
4. 中途弹出浏览器要求登录腾讯云，登录完回到黑色窗口等它跑完

---

## 第 ⑤ 步：拿网址发出去

1. 左侧菜单 → **静态网站托管**
2. 页面中部能看到 **默认域名**，形如 `https://jieqi-yaoshan-1a2b3c.tcloudbaseapp.com`
3. 这个就是发给用户访问的网址

回到第 ② 步的白名单，把这个域名补进去。

---

## 上线后自检清单

打开网址，逐项确认：

| 检查项 | 正常表现 |
|---|---|
| 页面能打开、不白屏 | 显示"节气药膳师 · 专业版"、节气列表、食材数量 |
| **后端连通** | 页面底部显示"后端 API 数据源 · 154 道膳方 · 207 味食材"。<br>如果显示"本地副本"或 195/133，说明后端没连上 |
| 生成席单可用 | 生成页选节气+体质 → 出一整套席单，**带"安全校验汇总"**<br>（没有这个模块说明走了本地降级） |
| 我的方案能存 | 生成后点「💾 保存方案」，再点头顶「👤 我的方案」能看到刚存的记录 |
| 刷新后还在 | 关掉浏览器重开，方案还在（说明登录态持久化正常） |

> 一眼判断是否后端生效的小技巧：**食材数 207 = 后端；195 = 本地降级**。

---

## 费用防护（务必做）

按量计费最大的风险不是花钱，是没人盯着花。

1. 云开发控制台 → **额度监控**，给「云函数调用次数」「数据库读次数」各设一个日限额（比如云函数 5000 次/天）
2. 腾讯云控制台右上角 → **费用** → **余额告警**，阈值设 100 元
3. 头一个月每周看一眼账单 → 控制台 **费用中心** → 账单详情

我们对 data/*.json 做了冷启动建库优化，且没有常驻进程 —— 没人访问时不产生任何费用。

---

## 常见故障

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 页面白屏 | 前端没传上去 | 重跑 bat，确认 hosting deploy 成功 |
| 能开页面但食材是 195 | 后端没连上 | 检查 `apiBase` 有没有填对、HTTP 访问服务有没有建 |
| 「我的方案」点开空的，控制台报 401 | 匿名登录没开，或报网络错误 | 第 ② 步 |
| 控制台报"域名不在白名单" | WEB 安全域名没加 | 第 ② 步第 5 条，把当前访问域名加进去 |
| 云函数调用超时（首次） | 冷启动要从 JSON 建库 | 已把 timeout 设到 60 秒；第二次就快了 |
| 「我的方案」按钮不显示 | 云 SDK 没加载成功（被墙/网络慢） | 不影响其余功能；换网络重试即可 |

---

## 回滚

出问题要退回上一版：

- 前端改坏了：本地 `git checkout` 到好的版本，重跑 bat（静态站会整个覆盖）
- 云函数改坏了：控制台 → 云函数 → `yaoshan-api` → **版本** → 切回历史版本
