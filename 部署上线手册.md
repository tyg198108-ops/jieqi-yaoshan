# 部署上线手册（腾讯云开发 · 零命令行版）

> 全程在网页点鼠标，不用装任何软件、不用打命令。
> 你的环境：`jieqi-yaoshan-d7gd6ypfscd10299f`（上海）
> 你的网址：`https://jieqi-yaoshan-d7gd6ypfscd10299f-1308818540.tcloudbaseapp.com`

已经准备好的两个上传包（放在「中医药膳师培训课件 / 腾讯云上传包」文件夹里）：

| 文件名 | 大小 | 用途 |
|---|---|---|
| `云函数包.zip` | 87 KB | 后端规则引擎，负责安全校验和生成席单 |
| `静态网站包.zip` | 667 KB | 网页本身，用户看到的所有界面 |

**这两个包要分别传到两个不同的地方**，别传混了。

---

## 先做两件事：数据库和登录

### ① 建数据库集合（存用户方案用）

1. 左侧菜单 → **数据库**
2. 点 **新建集合**，名字填 `user_plans`，确定
3. 再建一个，名字填 `user_profiles`，确定
4. **关键：改权限**。点开每个集合 → 右上「权限设置」→ 改选 **仅创建者可读写**

> 不改权限，别人能看到你学员保存的方案。这一步不能省。

### ② 开匿名登录

1. 左侧菜单 → **身份认证**（有的版本叫「登录授权」）
2. 找到 **匿名登录**，打开开关
3. 顺手找到 **WEB 安全域名 / 安全来源**，把这两个加进去：
   - `https://jieqi-yaoshan-d7gd6ypfscd10299f-1308818540.tcloudbaseapp.com`
   - `http://localhost:8000`

> 匿名登录 = 用户不用注册也能存方案。关掉的话，访客打开就存不了东西。

---

## 第三步：上传后端云函数

1. 左侧菜单 → **云函数**
2. 点 **新建云函数**
3. 填这些内容：

| 项目 | 填什么 |
|---|---|
| 函数名称 | `yaoshan-api`（必须一模一样） |
| 运行环境 | **Python 3.9**（没有就选 Python 3.7 或更高） |
| 函数内存 | 256 MB |
| 执行超时时间 | **60 秒**（默认 3 秒会不够用，第一次生成要建库） |
| 创建方式 / 上传方式 | **本地上传 zip 包**（有的是「上传代码包」「zip 上传」） |

4. 选中 `云函数包.zip`，确认创建
5. 等它变成「正常」状态

> 超时时间如果不改，用户生成第一份席单时会超时失败。这是最容易漏的一项。

---

## 第四步：给后端一个网址（最关键）

云函数本身没有对外网址，必须挂到 **HTTP 访问服务** 上，这是新手最常卡住的一步。

1. 左侧菜单 → **HTTP 访问服务**（也可能叫「HTTP 网关」「域名关联资源」，在靠下的位置）
2. 点 **新建**
3. **路径**填 `/api/*`
4. **关联资源**选：云函数 → `yaoshan-api`
5. 确定

建好后，页面上会显示一个域名。**把完整域名抄下来发我**（形如 `https://jieqi-yaoshan-xxxx.service.tcloudbase.com`）。

> 找不到这个菜单？用控制台**最顶部的搜索框**搜「HTTP」，能直接跳过去。

**不想抄也没关系**——我已经把你给的静态托管域名和两种常见的 HTTP 域名都预先写进配置了，页面会自己一个个试，试到通的为止。所以这一步你配完，多半直接就能用。

---

## 第五步：上传网页

1. 左侧菜单 → **静态网站托管**
2. 点「文件管理」或直接找 **上传** 按钮
3. 上传 `静态网站包.zip`（有的版本要先删除旧的 `index.html`，没有就不用管）
4. 上传完之后，如果改动没立刻生效，去同页面的「节点缓存配置」点一下**刷新缓存**（CDN 有时要等 1–3 分钟）

然后在同一个页面看 **默认域名**，就是发给用户访问的那个网址。

---

## 上线后怎么一眼看出成功没

打开你的网址，拉到页面**最底部**看食材数字：

| 看到的内容 | 说明 |
|---|---|
| `154 道膳方 · 207 味食材` | ✅ **后端通了**，完整功能可用 |
| `本地副本 · 195 味食材` | ❌ 后端没连上，掉的降级模式，生成不出合规席单 |

其他要确认的：

- 生成页选节气 + 体质 → 出一整套席单，**结果页要有「安全校验汇总」模块**（没有就是降级了）
- 生成后点「💾 保存方案」→ 点头顶「👤 我的方案」能看到刚存的记录
- 关掉浏览器重开，方案还在

---

## 常见故障

**页面打不开 / 404**
静态托管里没有 `index.html`。确认上传时选的是压缩包而不是解压后的文件夹。

**食材数显示 195，不是 207**
后端没连上。按顺序查：① 云函数状态是不是「正常」；② HTTP 访问服务有没有配 `/api/*`；③ 域名对不对（最稳的办法：把浏览器地址栏改成 `你的域名/api/health`，能返回一串 JSON 就说明后端通了）。

**生成一直转圈或报错**
大概率是云函数执行超时。回到第三步，把超时时间改成 60 秒，内存调到 256MB 以上。

**保存方案报错**
① 数据库两个集合建了没；② 权限改成「仅创建者可读写」了没；③ 匿名登录开了没。三样缺一不可。

**后端返回 `{"detail":"yaoshan-api 启动(import) 阶段错误"}`**
返回值里会带 `error` 和 `traceback_tail`，把那一整段复制给开发看，一眼就能定位。
绝大多数情况是某个依赖没打进包 —— 新版代码已加入**缺包自愈**：函数会自己把
缺的包装到 `/tmp` 再重试一次，所以**再访问一次往往就好了**；仍失败再取错误信息。

---

## 费用防护（务必做）

1. 右上角头像 → **费用中心** → 设置**余额告警**（比如低于 50 元提醒）
2. 云开发控制台 → **环境** → **额度监控**，给云函数调用次数、流量、数据库读写各设一个日上限

按量付费不怕花钱，怕的是没人管着花。

---

## 以后想更新网站

改了代码之后：

- 命令行路线：跑 `_build_cloud.py` 再用 `tcb` 上传
- 零命令行路线：让我重新打一次包（执行 `python _zip_for_console.py`），再去控制台重复第五步上传

只改了 `js/cloud-config.js` 这类配置的话，其实不用重新打包静态站——在浏览器控制台执行下面这句就能临时改（刷新失效）：

```js
localStorage.setItem('YS_API_BASE', 'https://你的新域名')
```
